# Generated. scripts/build.sh rewrites all of it, including template.html's
# output. The one source file in there, template.html, is 1,500 lines of
# hand-tuned markup and CSS that Prettier would rewrite wholesale.
dashboard/

# Build intermediates and local tooling state.
build/
node_modules/
.netlify/
.playwright-mcp/

# Working notes: audits, architecture, and the plans behind each change. Nobody
# ships these, and a formatting failure in one has already blocked a gate that
# was otherwise green. Their words matter; their line breaks do not.
docs/

# Lockfiles are written by their own tools.
package-lock.json
deno.lock

# Generated from the schemas by json-schema-to-typescript.
types/

# Written by scripts/build_md.py on every build. Formatting it only makes the
# next build undo the formatting and fail the check.
netlify/edge-functions/lib/md-routes.ts
