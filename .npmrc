# A contributor on the wrong Node gets an error naming the version this repo
# needs, instead of a confusing failure somewhere inside the build.
engine-strict=true

# Ranges drift. The version that passed CI is the version everyone installs.
save-exact=true
